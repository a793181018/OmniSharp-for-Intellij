package com.omnisharp.intellij.projectstructure.model;

/**
 * 项目结构节点类型枚举
 */
public enum NodeType {
    SOLUTION,
    PROJECT,
    FOLDER,
    FILE,
    REFERENCES,
    PROJECT_REFERENCES,
    PACKAGE_REFERENCES,
    ASSEMBLY_REFERENCES
}